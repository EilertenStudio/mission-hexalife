[tools]
# -----------------------------------------------------------------------------
act = "latest"
#gh = "latest"
# -----------------------------------------------------------------------------
npm = "11.6.0"
node = "22"


# -----------------------------------------------------------------------------
#[tasks.'website:tasks']
#depends = [
#    'mise:tasks website {{cwd}}'
#]
#hide = true
#quiet = true

# -----------------------------------------------------------------------------
#[tasks."website:core"]
#description = "Access to core package"
#dir = "{{cwd}}/packages/core"
#run = "mise r website:core:{{arg(var=true, default='tasks')}}"

# -----------------------------------------------------------------------------
[tasks."build"]
description = "Build project artifacts"
run = "npm run build"


# -----------------------------------------------------------------------------
[tasks."server:start"]
description = "Start server"
run = "npm run dev"

# -----------------------------------------------------------------------------
[tasks.'github:workflow']
description = "Access to GitHub Actions workflow of the package"
dir = "{{cwd}}"
run = [
  'act -W .github/workflows/mission-hexalife-website.yml {{arg(var=true, default="")}}'
]

[tasks.'github:workflow:validate']
description = "Validate GitHub Actions workflow of the package"
depends = [
  'github:workflow --dryrun'
]


